Linux Only so far. As I forgot how to use windows.

It Uses symbolic links.

Edit config by editing the Script.

Contributions that would port this to windows would be accepted as long as they are in .bat format.
